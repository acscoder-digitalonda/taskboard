# Supabase — required
# Get these from: https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key — for server-side API routes (never expose to client)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Dev bypass — set to "true" to skip Google Auth during development
NEXT_PUBLIC_DEV_BYPASS_AUTH=true
# NEXT_PUBLIC_DEV_BYPASS_USER_ID=9ccc8eb5-7690-49c3-8f42-c09f083e6c37

# Webhook secret — shared secret for inbound email, OpenClaw, and internal service calls
# WEBHOOK_SECRET=your-random-secret-here

# Team / org branding — used in auto-reply signatures and fallback email
NEXT_PUBLIC_TEAM_NAME=My Team

# Gmail — service account for sending emails
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}
# GMAIL_SEND_AS=team@example.com

# Twilio — for WhatsApp notifications (optional)
# TWILIO_ACCOUNT_SID=your-account-sid
# TWILIO_AUTH_TOKEN=your-auth-token
# TWILIO_WHATSAPP_FROM=whatsapp:+14155238886

# Google OAuth is configured in the Supabase dashboard, not here.
# See: https://supabase.com/dashboard/project/_/auth/providers
